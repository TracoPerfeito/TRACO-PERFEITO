<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Painel de Denúncias Comentários</title>
  <link rel="stylesheet" href="/css/pag-adm.css">
  <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Mo… rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swa… rel="stylesheet">
  
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/simple-notify@0.5.5/dist/simple-notify.min.css" />
   <script src="https://cdn.jsdelivr.net/npm/simple-notify@0.5.5/dist/simple-notify.min.js"></script>
   <script src="/js/simple-notify.js"></script>

  <link href='https://unpkg.com/boxicons@2.0.7/css/boxicons.min.css' rel='stylesheet'>
  <!-- <link rel="stylesheet" href="/css/adm-lista-denuncias-comentarios.css"> -->

  <link rel="stylesheet" href="/css/adm-lista-denuncias-comentario.css">

    <!-- Ícones Lucide -->
  <script src="https://unpkg.com/lucide@latest"></script>

  <!-- Ícones Remix -->
   <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet">


 
 


</head>
<body>
 
   <%  if (dadosNotificacao) { %>
        <script>
                notify("<%= dadosNotificacao.titulo%>","<%= dadosNotificacao.mensagem%>", "<%= dadosNotificacao.tipo%>", "top right");
                                   
        </script>
    <% } %>
      <!--INICIO DO MENU TOP ADM -->
 
    <%- include('../partials/menu_top_adm.ejs'); %>
 
    <!--FIM DO MENU TOP ADM  -->
 
 
    <!-- MENU LATERAL DO ADM -->
 
    <%- include('../partials/menu_lateral_adm.ejs'); %>
 
    <!-- MENU LATERAL DO ADM -->
 
 
 
    <h1 class="titulo-denuncias">📋 Denúncias de comentários</h1>
 
 
  <section class="container">
    <% if (denuncias.length > 0) { %>
      <% denuncias.forEach(denuncia => { %>
        <section class="card">
          <section class="card-header">
            <h2>Denúncia #<%= denuncia.ID_DENUNCIA %></h2>
            <span class="status <%= denuncia.STATUS %>">
              <%= denuncia.STATUS %>
            </span>
          </section>

          <section class="card-body">
            <p><span class="label">Id Usuário Denunciante:</span> <%= denuncia.ID_USUARIO_DENUNCIANTE || 'Anônimo' %></p>
            <p><span class="label">Id Usuário Denunciado:</span> <%= denuncia.ID_USUARIO_DENUNCIADO || 'Desconhecido' %></p>
            <p><span class="label">Comentário:</span> <%= denuncia.CONTEUDO_COMENTARIO %></p>
            <p><span class="label">Motivo:</span> <%= denuncia.MOTIVO %></p>
            <p><span class="label">Data da denúncia:</span> 
              <%= new Date(denuncia.DATA_DENUNCIA).toLocaleString('pt-BR') %>
            </p>
          </section>

          <section class="card-footer">
           

            <!-- Botão que abre o modal -->
            <button 
              class="btn-btn-status" 
              data-id="<%= denuncia.ID_DENUNCIA %>" 
              data-usuario="<%= denuncia.ID_USUARIO_DENUNCIADO %>">
              <i class="ri-edit-2-fill"></i>Status
            </button>
          </section>
        </section>
      <% }) %>
    <% } else { %>
      <p style="text-align: center; font-size: 18px; grid-column: 1 / -1;"> Nenhuma denúncia encontrada</p>
    <% } %>


      <!-- MODAL DE ALTERAR STATUS -->
<!-- MODAL DE ALTERAR STATUS -->
<section id="modalStatus" class="modal">
  <section class="modal-content">
    <h2>Alterar Status da Denúncia</h2>

    <form id="formStatus" method="POST">
      <input type="hidden" name="idDenuncia" id="idDenunciaInput">
      <input type="hidden" name="idUsuario" id="idUsuarioInput">

      <label for="acao">Ação:</label>
      <select id="acao" name="acao">
        <option value="">Selecione...</option>
        <option value="suspender">Suspender Usuário</option>
        <option value="descartar">Descartar Denúncia</option>
      </select>

      <section id="opcoesSuspensao" style="display:none;">
        <p>O usuário será suspenso.</p>
      </section>

      <section class="botoes-modal">
        <button type="submit" class="btn-confirmar">Confirmar</button>
        <button type="button" onclick="fecharModal()" class="btn-cancelar">Cancelar</button>
      </section>
    </form>
  </section>
</section>
 </section>
 
<script>
document.addEventListener("DOMContentLoaded", () => {
  const modal = document.getElementById("modalStatus");
  const btnsStatus = document.querySelectorAll(".btn-btn-status");
  const acao = document.getElementById("acao");
  const opcoesSuspensao = document.getElementById("opcoesSuspensao");
  const form = document.getElementById("formStatus");

  // Abrir modal e preencher IDs
  btnsStatus.forEach(btn => {
    btn.addEventListener("click", () => {
      const idDenuncia = btn.getAttribute("data-id");
      const idUsuario = btn.getAttribute("data-usuario");

      document.getElementById("idDenunciaInput").value = idDenuncia;
      document.getElementById("idUsuarioInput").value = idUsuario;

      modal.style.display = "block";
      acao.value = "";
      opcoesSuspensao.style.display = "none";
    });
  });

  // Fechar modal
  function fecharModal() {
    modal.style.display = "none";
    acao.value = "";
    opcoesSuspensao.style.display = "none";
  }
  window.fecharModal = fecharModal;
  window.addEventListener("click", e => { if (e.target === modal) fecharModal(); });

  // Mostrar aviso de suspensão
  acao.addEventListener("change", () => {
    opcoesSuspensao.style.display = acao.value === "suspender" ? "block" : "none";
  });

  // Mudar ação do form dependendo da opção
  form.addEventListener("submit", e => {
    if (!acao.value) {
      e.preventDefault();
      return alert("Selecione uma ação antes de confirmar.");
    }

    if (acao.value === "suspender") {
      form.action = "/adm/desativar-usuario/" + document.getElementById("idUsuarioInput").value;
      form.method = "POST"; // ou PUT se quiser
    } else if (acao.value === "descartar") {
      form.action = "/adm/excluir-denuncia-comentario";
      form.method = "POST";
    }
  });
});
</script>

    <script>
    lucide.createIcons();
  </script>

<script>


  const modal = document.getElementById("modalStatus");
const form = document.getElementById("formStatus");
const acao = document.getElementById("acao");

// Abrir modal
document.querySelectorAll(".btn-btn-status").forEach(btn => {
  btn.addEventListener("click", () => {
    const idDenuncia = btn.getAttribute("data-id");
    const idUsuario = btn.getAttribute("data-usuario");

    document.getElementById("idDenunciaInput").value = idDenuncia;
    document.getElementById("idUsuarioInput").value = idUsuario;

    acao.value = "";
    modal.style.display = "block";
  });
});

// Fechar modal
function fecharModal() { modal.style.display = "none"; }
window.addEventListener("click", e => { if (e.target === modal) fecharModal(); });

// Ajusta action do form dinamicamente
form.addEventListener("submit", e => {
  const acaoSelecionada = acao.value;
  if (!acaoSelecionada) {
    e.preventDefault();
    alert("Selecione uma ação antes de confirmar.");
    return;
  }

  if (acaoSelecionada === "suspender") {
    form.action = "/adm/desativar-usuario/" + document.getElementById("idUsuarioInput").value;
  } else if (acaoSelecionada === "descartar") {
    form.action = "/adm/excluir-denuncia-comentario";
  }
});

</script>

      <!-- LINK DO MENU LATERAL -->
      <script src="/js/menu-lateral-adm.js"></script>

    <!-- LINK DAS NOTIFICAÇÕES -->
      <script src="js/simple-notify.js"></script>


</body>
</html>